# Core (required)
numpy>=1.24
rasterio>=1.3
requests>=2.28

# Visual quicklook output (optional but recommended)
matplotlib>=3.6

# Reading a GLiM lithology vector directly (optional; only for --glim vector)
# fiona>=1.9
# geopandas>=0.13
