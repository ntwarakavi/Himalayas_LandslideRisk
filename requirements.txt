# Core (required)
numpy>=1.24
rasterio>=1.3
requests>=2.28

# Vector I/O. Required: every landslide inventory is a shapefile, the regional
# sweep reads its states and provinces through it, and so does the exposure
# fallback. Also reads the full-resolution GLiM lithology geodatabase.
fiona>=1.9

# Optional ------------------------------------------------------------------
# Two-panel quicklook PNGs. Without it, quicklooks are skipped and everything
# else runs.
matplotlib>=3.6

# Faster PNG writing for the web maps. Without it a built-in writer is used.
pillow>=9.0

# Test runner.
pytest>=7.0
